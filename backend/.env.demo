# ==========================================
# CONFIGURAÇÃO AMBIENTE DEMO
# NÃO usar em produção real!
# ==========================================

# Servidor
PORT=3001
NODE_ENV=production

# Banco de Dados PostgreSQL (será configurado automaticamente pelo Render)
# DATABASE_URL será injetado pelo Render via render-demo.yaml

# Autenticação DEMO
JWT_SECRET=demo_jwt_secret_key_12345_not_for_production_use_demo_only
ADMIN_DEFAULT_PASSWORD=Demo@2025

# Upload
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880

# CORS (ambientes demo)
CORS_ORIGIN=https://demo.balek.de,https://balek-demo-frontend.onrender.com
